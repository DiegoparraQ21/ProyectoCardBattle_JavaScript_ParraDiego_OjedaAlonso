{
  "name": "card-battle-arena",
  "version": "1.0.0",
  "description": "Prototipo académico Card Battle Arena — temática Naruto (fan-made, no comercial).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch src/data/db.json --port 3000",
    "start": "json-server --host 0.0.0.0 --watch src/data/db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "0.17.4"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}